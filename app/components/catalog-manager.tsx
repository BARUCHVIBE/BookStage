"use client";

/* eslint-disable jsx-a11y/label-has-associated-control -- os controles são aninhados visualmente em labels gerados pelo editor. */

import { Save } from "lucide-react";
import { useEffect, useState } from "react";

type Organization={id:string;name:string;slug:string;email:string;phone?:string;document?:string;website?:string;instagram?:string;logo?:string;description?:string;role:string};
type Artist={id:string;name:string};
type ArtistFields={name:string;slug:string;photoUrl:string;coverUrl:string;genre:string;description:string;baseCity:string;showFormats:string;videoUrls:string;instagram:string;spotify:string;youtube:string;publicMaterials:string;isPublic:boolean};
const emptyArtist:ArtistFields={name:"",slug:"",photoUrl:"",coverUrl:"",genre:"",description:"",baseCity:"",showFormats:"",videoUrls:"",instagram:"",spotify:"",youtube:"",publicMaterials:"",isPublic:false};

export function CatalogManager({organization,artists,canManage}:{organization:Organization;artists:Artist[];canManage:boolean}){
  const [selectedId,setSelectedId]=useState(artists[0]?.id||""),[form,setForm]=useState(emptyArtist),[description,setDescription]=useState(organization.description||""),[notice,setNotice]=useState("");
  useEffect(()=>{let active=true;if(!selectedId)return;fetch(`/api/artists/${selectedId}`).then(response=>response.json() as Promise<{artist?:Partial<ArtistFields>&{name:string};error?:string}>).then(data=>{if(!active||!data.artist)return;const artist=data.artist;setForm({name:artist.name,slug:artist.slug||"",photoUrl:artist.photoUrl||"",coverUrl:artist.coverUrl||"",genre:artist.genre||"",description:artist.description||"",baseCity:artist.baseCity||"",showFormats:artist.showFormats||"",videoUrls:artist.videoUrls||"",instagram:artist.instagram||"",spotify:artist.spotify||"",youtube:artist.youtube||"",publicMaterials:artist.publicMaterials||"",isPublic:Boolean(artist.isPublic)});setNotice("")});return()=>{active=false}},[selectedId]);
  async function saveArtist(event:React.FormEvent){event.preventDefault();const response=await fetch(`/api/artists/${selectedId}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(form)}),data=await response.json() as {error?:string;slug?:string};if(!response.ok){setNotice(data.error||"Não foi possível salvar o catálogo.");return}setForm(current=>({...current,slug:data.slug||current.slug}));setNotice("Dados públicos do artista atualizados.")}
  async function saveOrganizationDescription(){const response=await fetch(`/api/organizations/${organization.id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({...organization,description})});setNotice(response.ok?"Apresentação da organização atualizada.":"Não foi possível atualizar a apresentação.")}
  const field=(key:Exclude<keyof ArtistFields,"isPublic">,label:string,placeholder="")=><label>{label}<input disabled={!canManage} value={form[key]} placeholder={placeholder} onChange={event=>setForm({...form,[key]:event.target.value})}/></label>;
  return <section className="catalog-manager"><div className="page-heading"><div><p className="eyebrow">Catálogo público</p><h1>Vitrine comercial</h1><p>Controle somente as informações autorizadas para visitantes.</p></div><a className="button button-secondary" href={`/catalogo/${organization.slug}`} target="_blank" rel="noreferrer">Abrir catálogo público</a></div>{notice&&<div className="notice">{notice}</div>}<section className="catalog-settings-card"><div className="section-heading"><div><p className="eyebrow">Organização</p><h2>Apresentação do catálogo</h2></div>{canManage&&<button className="button button-secondary" onClick={saveOrganizationDescription}>Salvar apresentação</button>}</div><label>Descrição pública<textarea disabled={!canManage} value={description} onChange={event=>setDescription(event.target.value)} placeholder="Apresente o escritório, label ou agência ao mercado."/></label><small className="catalog-url">URL pública: /catalogo/{organization.slug}</small></section><section className="catalog-settings-card"><div className="section-heading"><div><p className="eyebrow">Artistas</p><h2>Informações públicas</h2></div><label className="catalog-artist-picker">Artista<select value={selectedId} onChange={event=>setSelectedId(event.target.value)}>{artists.map(artist=><option value={artist.id} key={artist.id}>{artist.name}</option>)}</select></label></div>{artists.length?<form onSubmit={saveArtist}><label className="publish-toggle"><input type="checkbox" disabled={!canManage} checked={form.isPublic} onChange={event=>setForm({...form,isPublic:event.target.checked})}/><span><b>Publicar este artista</b><small>Somente artistas ativos e publicados aparecem no catálogo.</small></span></label><div className="catalog-form-grid">{field("name","Nome artístico *")}{field("slug","URL do artista","artista-x")}{field("genre","Gênero musical")}{field("baseCity","Cidade-base")}{field("photoUrl","URL da foto")}{field("coverUrl","URL da capa")}{field("instagram","Instagram")}{field("spotify","Spotify")}{field("youtube","YouTube")}</div><label>Descrição pública<textarea disabled={!canManage} value={form.description} onChange={event=>setForm({...form,description:event.target.value})}/></label><div className="catalog-form-grid public-list-fields"><label>Formatos de show <small>Um por linha</small><textarea disabled={!canManage} value={form.showFormats} onChange={event=>setForm({...form,showFormats:event.target.value})}/></label><label>Vídeos públicos <small>Uma URL por linha</small><textarea disabled={!canManage} value={form.videoUrls} onChange={event=>setForm({...form,videoUrls:event.target.value})}/></label><label>Materiais públicos <small>Uma URL por linha</small><textarea disabled={!canManage} value={form.publicMaterials} onChange={event=>setForm({...form,publicMaterials:event.target.value})}/></label></div><div className="catalog-manager-actions">{form.isPublic&&form.slug&&<a href={`/catalogo/${organization.slug}/${form.slug}`} target="_blank" rel="noreferrer">Visualizar página pública</a>}{canManage&&<button className="button button-primary"><Save size={16}/>Salvar artista</button>}</div></form>:<div className="public-empty">Cadastre um artista antes de configurar o catálogo.</div>}</section></section>;
}
